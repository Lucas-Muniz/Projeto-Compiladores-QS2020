programa

  declare numero a, b .
  declare texto  t1 .

  leia(a).
  leia(b).
  
  a := 1 + 2 * 3 / b .
  a := (1+3*8)/(3/(4)). 

  se ( a > b ) entao {
     escreva (a).
  }
  senao {
     escreva (b).
  }
  
  enquanto (a < b){
     escreva(a).
     escreva("TESTE texto 45 ").
  }
  
  

fimprog.